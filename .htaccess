# Force index.html as the default document
DirectoryIndex index.html

# Enable URL rewriting
RewriteEngine On

# If the request is for the root directory, serve index.html
RewriteRule ^$ /index.html [L]
